#include <stdio.h>

#include "bigint.h"

BigInt* normalize(BigInt *num) {
  if(num != NULL) {
    return num;
  }
  else return NULL;
}

 Kiểm tra concept

void printBigInt(BigInt *num) {

}
